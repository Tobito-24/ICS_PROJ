<?xml version="1.0" encoding="utf-8" ?>
<views:ContentPageBase xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                       xmlns:texts="clr-namespace:VUTIS2.App.Resources.Texts"
                       xmlns:vm="clr-namespace:VUTIS2.App.ViewModels"
                       xmlns:m="clr-namespace:VUTIS2.BL.Models;assembly=VUTIS2.BL"
                       xmlns:views="clr-namespace:VUTIS2.App.Views"
                       x:DataType="vm:ActivityDetailViewModel"
                       x:Class="VUTIS2.App.Views.Activity.ActivityDetailView"
                       x:Name="ActivityDetailViewRoot">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackLayout Grid.Column="0" Margin="50" Spacing="10" HorizontalOptions="FillAndExpand">
            <Editor Placeholder="Activity Detail"
                    HeightRequest="100"
                    AutoSize="TextChanges" />

            <HorizontalStackLayout Spacing="10">
                <Label Text="Start date:" VerticalOptions="Center" />
                <DatePicker />
            </HorizontalStackLayout>

            <HorizontalStackLayout Spacing="10">
                <Label Text="End date:" VerticalOptions="Center" />
                <DatePicker />
            </HorizontalStackLayout>

            <HorizontalStackLayout Spacing="10">
                <Label Text="Room:" VerticalOptions="Center" />
                <Entry Placeholder="Room Name" />
            </HorizontalStackLayout>
        </StackLayout>
    </Grid>
</views:ContentPageBase>
